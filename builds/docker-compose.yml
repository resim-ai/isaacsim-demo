services:
  isaacsim:
    image: ${ISAACSIM_IMAGE}
    privileged: true
    labels:
      - resim.ai/containerPriority=1
    environment:
      - NVIDIA_DRIVER_CAPABILITIES=all
    restart: unless-stopped
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities:
                - gpu
                - nvidia
  nav2:
    image: ${NAV2_IMAGE}
    labels:
      - resim.ai/isTestOrchestrator=true
      - resim.ai/containerPriority=2
